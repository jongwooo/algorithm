#include <iostream>

using namespace std;

int main(void) {
    cin.tie(0) -> sync_with_stdio(0);
    int a;
    int b;
    cin >> a >> b;
    cout << a << " + " << b << " = " << a + b << '\n';
    return 0;
}
